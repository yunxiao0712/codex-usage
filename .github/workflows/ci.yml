name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6
      - name: Build app bundle
        run: ./build.sh
      - name: Run self-tests
        run: ./.artifacts/Codex\ Usage\ Strip.app/Contents/MacOS/CodexUsageStrip --self-test
      - name: Verify code signature
        run: codesign --verify --deep --strict --verbose=2 ./.artifacts/Codex\ Usage\ Strip.app
