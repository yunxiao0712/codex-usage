# 安全策略

## 支持范围

仅最新稳定版本接受安全修复。发现漏洞时，请不要先公开 Issue；应通过仓库维护者公布的私密安全报告渠道提交。

## 发布安全要求

- 所有公开发布包必须使用 Apple Developer ID 签名并完成 notarization。
- Sparkle 更新包必须使用独立保存的 EdDSA 私钥签名。
- `SUFeedURL` 必须使用 HTTPS。
- 私钥、证书和 Apple 凭据不得进入仓库、构建日志或 Release 附件。
- 自动更新发布前必须从上一稳定版本完成一次真实升级验证。

尚未满足以上要求的构建只能标记为本地测试包，不应公开分发。
