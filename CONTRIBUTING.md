# 贡献指南

## 设计边界

QuotaGlow 只解决一个问题：让用户在桌面上低打扰地看到 Codex 每周总额度。新增能力应保持常驻轻量、本地优先和界面克制，不扩展为通用聊天客户端或多账号管理器。

## 开发流程

1. 从 `main` 创建短分支。
2. 修改后运行 `make test`。
3. 涉及额度解析时补充脱敏 fixture，覆盖“总额度优先、模型额度忽略”的边界。
4. 涉及界面时附上前后截图，并检查浅色/深色外观。
5. 提交信息说明问题根因和验证方式。

## 代码要求

- 使用 Swift 和原生 AppKit，避免为小功能引入大型依赖。
- 不记录额度响应正文或用户路径。
- 不把本地绝对路径、账号信息、证书或密钥写入源码。
- 兼容 macOS 14 及以上版本。
